# --- web ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- data ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# --- config / validation ---
pydantic==2.10.4
pydantic-settings==2.7.0

# --- scheduling (workers: sweeper, outbox dispatcher) ---
apscheduler==3.11.0

# --- auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- assistant ---
# Groq is OpenAI-compatible, so the OpenAI SDK pointed at Groq's base URL is
# the whole client. No vendor-specific package.
openai==3.3.1

# --- notifications ---
resend==2.5.1
qrcode[pil]==8.0

# --- templating (email bodies) ---
jinja2==3.1.5
